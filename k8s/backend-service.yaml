apiVersion: v1
kind: Service
metadata:
  name: ai-k8s-agent-backend
  namespace: ai-k8s-agent
spec:
  selector:
    app: ai-k8s-agent-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
