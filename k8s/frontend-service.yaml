apiVersion: v1
kind: Service
metadata:
  name: ai-k8s-agent-frontend
  namespace: ai-k8s-agent
spec:
  selector:
    app: ai-k8s-agent-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
